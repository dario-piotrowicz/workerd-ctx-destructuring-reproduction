{
  "name": "ctx-destructuring-repro",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev-waitUntil": "wrangler dev --config wrangler.waitUntil.toml",
    "dev-waitUntil-broken": "wrangler dev --config wrangler.waitUntil.broken.toml",
    "dev-passThroughOnException": "wrangler dev --config wrangler.passThroughOnException.toml",
    "dev-passThroughOnException-broken": "wrangler dev --config wrangler.passThroughOnException.broken.toml",
    "deploy-waitUntil": "wrangler deploy --config wrangler.waitUntil.toml",
    "deploy-waitUntil-broken": "wrangler deploy --config wrangler.waitUntil.broken.toml",
    "deploy-passThroughOnException": "wrangler deploy --config wrangler.passThroughOnException.toml",
    "deploy-passThroughOnException-broken": "wrangler deploy --config wrangler.passThroughOnException.broken.toml"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "wrangler": "^3.60.3",
    "vitest": "1.5.0"
  }
}